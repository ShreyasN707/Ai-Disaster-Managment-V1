# Server Configuration
PORT=10000
NODE_ENV=production
ORIGIN=http://localhost:3000

# Database Configuration
# IMPORTANT: Replace with your actual MongoDB connection string
# For MongoDB Atlas (recommended for production):
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/disaster-management?retryWrites=true&w=majority
# For local MongoDB (development only):
# MONGO_URI=mongodb://localhost:27017/disaster-management

# WARNING: Do NOT set MONGO_URI to just a number or invalid string like "234"
# This will cause connection errors. Always use a proper MongoDB connection string format.

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1d
ADMIN_REGISTRATION_CODE=change_this_admin_code

# Email Configuration (Gmail)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=qebt gpnw fgla aast

# Twilio Configuration (SMS)

# Logging
LOG_LEVEL=info

# File Uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# ML Service Configuration
ML_MODEL_PATH=./tfjs_model
ML_PREDICTION_ENDPOINT=http://localhost:5000
ENABLE_ML_SERVICE=true

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
FRONTEND_BUILD_DIR=./frontend/dist

# Production Settings
CORS_ORIGINS=http://localhost:3000,http://localhost:4000
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
